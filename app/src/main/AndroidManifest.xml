<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tac.iparttimejob">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.GiveAndReceiveJobs.AppMain">

        </activity>

        <activity android:name=".UI.RegisterAndLogin.Register">
        </activity>

        <activity android:name=".UI.GiveAndReceiveJobs.PostJobs">

        </activity>
        <activity android:name=".UI.GiveAndReceiveJobs.JobContentForGiver">

        </activity>

        <activity android:name=".UI.MyManager.SetAccountInfo">

        </activity>

        <activity android:name=".UI.Admin.AppMainForAdmin">

        </activity>

        <activity android:name=".UI.GiveAndReceiveJobs.JobContentForReceiver">

        </activity>

        <activity android:name=".UI.RegisterAndLogin.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>